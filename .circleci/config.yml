version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  build:
    jobs:
      - cypress/install
      - cypress/run:
          executor: cypress/browsers-chrome69
          browser: chrome

          requires:
            - cypress/install
          record: true
          parallel: true
          parallelism: 1
          group: 1 machines
